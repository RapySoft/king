enum StatusDefault { initial, loading, success, failed }

enum StatusSavedDefault { initial, loading, success,successSaved, failed }



enum EnumStatusLogin {
  initial,
  loading,
  success,
  successRecoverPassword,
  update,
  failed,
  failedBiometric
}

enum EnumTypePermission {
  client,
  regCredit,
  credit,
  payment,
  expenses,
  enterExpense,
  collector,
  location,
  state,
  historyExpense,
  penalty
}
